# DHCP client on eth0 via busybox udhcpc
# -f foreground (finit requires non-daemon)
# -i eth0 interface
# -s default script for IP/route/DNS setup
service [S12345789] /sbin/udhcpc -f -i eth0 -s /usr/share/udhcpc/default.script -- DHCP client
